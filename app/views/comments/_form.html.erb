<%= form_for :comment, :html => {:name => 'new_comment'} do |f| %>
  <%= error_messages_for :comment %>
  <%= f.label :author %>
  <%= f.text_field :author %>
  <%= f.label :content %>
  <%= f.text_area :content, :rows => 10, :cols => 40 %>
  <p>
    <%= f.submit %>
  </p>
<% end %>

